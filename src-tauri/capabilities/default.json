{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capabilities for the main window — covers core window APIs plus auto-update, persistent settings store, and app metadata (getVersion, restart).",
  "windows": [
    "main"
  ],
  "permissions": [
    "core:default",
    "updater:default",
    "store:default",
    "app:default"
  ]
}
